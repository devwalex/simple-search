import React from "react";

const SearchBox = () => {
  return (
    <div className="pa3">
      <input className="pa3 ba b--blue bg-lightest-blue" type="search" placeholder="Search Movies" />
    </div>
  );
};

export default SearchBox;
